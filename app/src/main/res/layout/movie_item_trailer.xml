<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/movie_trailers_container"
    android:layout_width="@dimen/rv_movie_trailer_width"
    android:layout_height="@dimen/rv_movie_trailer_height"
    android:background="@color/colorPrimaryDark">

    <ImageView
        android:id="@+id/iv_movie_trailer_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="8dp"
        android:paddingEnd="8dp" />

    <ImageView
        android:id="@+id/iv_play_button"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/ic_play_button" />
</RelativeLayout>

